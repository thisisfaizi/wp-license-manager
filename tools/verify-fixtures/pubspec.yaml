name: wplm_verify_fixtures
description: >-
  Cross-language check for the Super Ledger contract fixtures: verifies every token with an
  independent Dart Ed25519 implementation, the way the app will.
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  cryptography: ^2.9.0
